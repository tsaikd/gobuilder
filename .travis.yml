language: go

go:
  - 1.7
  - 1.8
  - 1.9
  - tip

install:
  - go install . ./checkerror/vendor/...
  - gobuilder version -c ">=0.1.7" &>/dev/null || go get -u -v "github.com/tsaikd/gobuilder"

script:
  - gobuilder --check --test --all
